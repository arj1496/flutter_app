import 'package:flutter/cupertino.dart';
class Contacts{

  String title;
  String subtitle;
  IconData leadingIcon;
  IconData trailingIcon;

  Contacts(this.title, this.subtitle, this.leadingIcon, this.trailingIcon);

}